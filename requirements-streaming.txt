# Requirements for the real-time streaming server (server/, app.py).
# The upstream requirements.txt targets the offline inference.py; this set is
# for the live server (python 3.12, torch 2.11, tested on sm_120).

# --- torch stack ---
# sm_120 (Blackwell) needs CUDA >= 12.8 kernels. Drop the index and the +cu128
# tags on older cards if the default PyPI wheels already match your CUDA.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128    # required by omnivoice

# --- model code ---
transformers>=5.3,<6        # >=5.3 is omnivoice's floor
timm>=1.0.9,<2
omegaconf>=2.3.0
opencv-python>=4.10
face-alignment>=1.4.1       # only detection.sfd.FaceDetector is used
numpy>=2.4,<2.5             # <2.5 is forced by numba, a hard dep of face-alignment
pillow>=10.0
pyyaml>=6.0
tqdm>=4.66

# --- audio I/O ---
soundfile>=0.12
soxr>=0.5
av>=12.0

# --- server ---
gradio==6.20.*              # server/web.py builds on gradio.Server
fastapi>=0.115
uvicorn>=0.30
websockets>=13.0
python-dotenv>=1.0
openai>=1.40                # AsyncOpenAI against any OpenAI-compatible LLM endpoint

# --- speech: OmniVoice TTS + Whisper STT ---
omnivoice==0.2.1

# --- semantic end-of-turn (server/turn_detect.py) ---
onnxruntime>=1.20
